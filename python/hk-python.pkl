import "../hk.pkl" as hk
import "../version.pkl" as version

fix: Mapping<String, hk.Step> = new {
  ["[fix] python"] = version.Builtins.ruff
}

format: Mapping<String, hk.Step> = new {
  ["[format] python"] = version.Builtins.ruff_format
}

lint: Mapping<String, hk.Step> = new {
  ["[lint] python"] = version.Builtins.ty
}
