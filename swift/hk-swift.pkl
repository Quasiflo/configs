import "../hk.pkl" as hk
import "../version.pkl" as version

steps = new Mapping<String, hk.Step> {
  ["[fix] swift"] = version.Builtins.swiftlint
  ["[format] swift"] {
    glob = List("**/*.swift")
    check = "swiftformat --lint {{ files }}"
    fix = "swiftformat {{ files }}"
  }
}
