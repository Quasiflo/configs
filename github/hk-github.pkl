import "../hk.pkl" as hk
import "../version.pkl" as version

fix: Mapping<String, hk.Step> = new {
  ["[fix] github actions"] = (version.Builtins.zizmor) {
    check_diff = "zizmor --pedantic --no-progress {{ files }}"
    fix = "zizmor --pedantic --fix {{ files }}"
  }
}
