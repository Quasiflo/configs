import "../hk.pkl" as hk
import "../version.pkl" as version

steps = new Mapping<String, hk.Step> {
  ["[lint] dockerfile"] = (version.Builtins.hadolint) {
    glob = List("**/Dockerfile")
    check = "hadolint --config .config/generated/.hadolint.yaml {{ files }}"
  }
}
